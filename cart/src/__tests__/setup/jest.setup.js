// Jest setup file
beforeAll(async () => {
  // Setup runs before all tests
});

afterAll(async () => {
  // Cleanup after all tests
});

beforeEach(() => {
  // Reset mocks before each test
  jest.clearAllMocks();
});
